# dlpi_promiscon
`dlpi_promiscon` is a utility for Solaris/Illumos systems that sets a network interface into **promiscuous mode**.   In promiscuous mode, the interface receives all packets on the network segment, not just those addressed to it. This is useful for packet capture and network monitoring.
